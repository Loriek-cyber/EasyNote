<Window x:Class="EasyNote.View.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:EasyNote.View"
        Title="Input" SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        >
        <StackPanel Margin="15">
                <TextBlock Text="{Binding Message}" Margin="0 0 0 8"/>
                <TextBox Text="{Binding UserInput}" Width="250" Margin="0 0 0 10"/>
        
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Cancel" Width="70" Margin="0 0 8 0" Click="Cancel_Click"/>
                        <Button Content="OK" Width="70" Click="Ok_Click"/>
                </StackPanel>
        </StackPanel>
</Window>
